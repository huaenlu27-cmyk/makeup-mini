# miniapp（微信小程序示例）

说明：本文件夹为把 `web-mvp` 演示项目简化并移植为微信小程序结构的示例实现。可用微信开发者工具打开 `miniapp` 目录进行预览。

如何运行：
1. 安装并打开微信开发者工具。
2. 在工具中选择“导入项目”，选择本仓库的 `miniapp` 文件夹。
3. 在工具中预览或编译调试。

注意：此示例使用本地静态 JSON 数据，仅用于演示交互与页面结构，未集成后端 API 与鉴权逻辑。
